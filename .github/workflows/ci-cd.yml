name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    # Run verification weekly
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  validate:
    name: Validate JSON Data
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm install --only=dev
      
      - name: Validate JSON syntax
        run: |
          for f in data/*.json; do
            echo "Validating $f..."
            node -e "JSON.parse(require('fs').readFileSync('$f'))"
            echo "✓ $f is valid"
          done
          for f in data/banks/*.json; do
            node -e "JSON.parse(require('fs').readFileSync('$f'))" || exit 1
          done
          echo "✓ $(ls data/banks/*.json | wc -l) bank data files are valid"
      
      - name: Check required fields
        run: |
          node -e "
          const index = JSON.parse(require('fs').readFileSync('data/banks/index.json'));
          const fs = require('fs');
          if (!index.banks || !Array.isArray(index.banks) || index.banks.length === 0) throw new Error('Missing: index.banks');
          index.banks.forEach(b => {
            if (!fs.existsSync('data/banks/' + b.slug + '.json')) throw new Error('Missing per-bank file: ' + b.slug);
          });
          console.log('✓ Required fields present');
          console.log('✓ Total banks:', index.count || index.banks.length);
          "

  verify-urls:
    name: Verify Bank URLs
    runs-on: ubuntu-latest
    needs: validate
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Check URL accessibility
        run: |
          # Basic URL check - just verify structure
          node -e "
          const fs = require('fs');
          const index = JSON.parse(fs.readFileSync('data/banks/index.json'));
          let issues = [];
          let methods = 0;
          index.banks.forEach(entry => {
            const bank = JSON.parse(fs.readFileSync('data/banks/' + entry.slug + '.json'));
            if (!bank.website) issues.push(bank.name + ': missing website');
            bank.cards.forEach(card => {
              (card.blocking_methods || []).forEach(method => {
                methods++;
                if (method.url && !method.url.startsWith('http')) {
                  issues.push(bank.name + ': invalid URL for ' + method.channel);
                }
              });
            });
          });
          console.log('✓ Total blocking methods:', methods);
          if (issues.length > 0) {
            console.log('Issues found:');
            issues.forEach(i => console.log('  - ' + i));
          } else {
            console.log('✓ All URLs have valid structure');
          }
          "

  build:
    name: Build Site
    runs-on: ubuntu-latest
    needs: validate
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Verify HTML files exist
        run: |
          echo "Checking required files..."
          test -f index.html && echo "✓ index.html exists"
          test -f about.html && echo "✓ about.html exists"
          test -f policy.html && echo "✓ policy.html exists"
          test -f data/banks/index.json && echo "✓ data/banks/index.json exists"
      
      - name: Verify links in HTML
        run: |
          # Check for basic HTML structure
          grep -q "Card Block API" index.html && echo "✓ index.html has title"
          grep -q "data/banks/index.json" index.html && echo "✓ index.html references data"

  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: [validate, verify-urls, build]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

  notify:
    name: Notify on Changes
    runs-on: ubuntu-latest
    needs: [validate, deploy]
    if: always()
    
    steps:
      - name: Summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- JSON Validation: ${{ needs.validate.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- URL Verification: ${{ needs.verify-urls.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Build: ${{ needs.build.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Deploy: ${{ needs.deploy.result }}" >> $GITHUB_STEP_SUMMARY
